//#include <stdio.h> //include : 포함하다 (studio.h를 들고 옴) - 그 파일에 있는 코드를 쓸거임

//변수 : 수학시간때 x, y
//자료형 : 정수인지 문자열인지 주소인지 등등 문자인지 소수인지 등등 클래스 등등 void
// // /* */ : 주석, 컴퓨터가 무시함

//x : 변수 이름
//int는 자료형이면서, 정수일때 사용
//4byte 빌려주세요
//공간의 이름 - 주소 ex

//int x = 5;
//int* xx = &x;

//char y = 'c'; //y : 변수 이름, char은 자료형이면서, 문자일때 사용, 이거는 문자열은 사용 못 함 (ex char y = '사로로인싸')
//float z = 3.14f; //z : 변수 이름, float은 자료형이면서, 소수일때 사용

//int main() { //함수


    //출력문
    //printf("%d", x);
    //%d - int

    //printf("%c", y);
    //%c - char

    //printf("%f", z);
    //%f - float

    //printf("x : %d", x);

    //printf("x : %d, y : %c, z : %f", x, y, z);

    //printf("x : %d, y : %c, z : %f", x, y, z);

    //printf("%p", xx);

        //return 0;
//}